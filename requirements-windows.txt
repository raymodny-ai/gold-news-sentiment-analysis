# Windows-compatible version of requirements.txt
# Use this file if you encounter issues with the main requirements.txt on Windows

# Core dependencies (same as main)
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0

# Database - Windows compatible versions
sqlalchemy==2.0.23
alembic==1.13.1
# For Windows, try psycopg2 first (requires PostgreSQL dev tools)
# psycopg2-binary==2.9.7  # Uncomment this if psycopg2 doesn't work

# Alternative database options for Windows:
# pymysql==1.1.0  # If using MySQL
# pyodbc==4.0.39  # If using SQL Server

# Cache and task queue (same as main)
redis==5.0.1
celery==5.3.4

# Data processing (same as main)
pandas==2.1.4
numpy==1.24.3
scikit-learn==1.3.2

# Machine learning - Windows compatible versions
torch==2.1.1 --index-url https://download.pytorch.org/whl/cpu
transformers==4.36.2
xgboost==2.0.2
tensorflow==2.15.0

# NLP and sentiment analysis (same as main)
nltk==3.8.1
textblob==0.17.1
vaderSentiment==3.3.2

# HTTP client (same as main)
requests==2.31.0
aiohttp==3.9.1

# Financial data (same as main)
yfinance==0.2.28

# Environment management (same as main)
python-dotenv==1.0.0

# Logging and monitoring (same as main)
structlog==23.2.0

# Development and testing (same as main)
pytest==7.4.3
pytest-asyncio==0.21.1
httpx==0.25.2

# Streamlit dashboard (same as main)
streamlit==1.28.2
plotly==5.17.0
altair==5.1.2
